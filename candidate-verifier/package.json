{
  "name": "candidate-verifier",
  "version": "1.0.0",
  "description": "R22-E2C2 execution-plane worker: exact-SHA workspace materialization + Maven compile/test, extracted from the backend's R22-C candidate-verification module. Owns no secrets, no database, no GitHub write capability, no business decision-making.",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/candidate-verifier/src/server.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
